version: '3.9'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - media_data:/app/media
      - chroma_data:/app/chroma_db
    restart: unless-stopped

volumes:
  media_data:
  chroma_data: